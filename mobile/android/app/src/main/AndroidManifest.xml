<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- รับ FCM แทน service ของ plugin (subclass ส่งต่อให้ plugin ครบ) เพื่อทำเสียงออเดอร์ดังวน
             ต้องถอดตัวเดิมออก: มี MESSAGING_EVENT สองตัว Firebase จะเลือกตัวไหนก็ได้ -->
        <service
            android:name="io.capawesome.capacitorjs.plugins.firebase.messaging.MessagingService"
            tools:node="remove" />
        <service
            android:name=".OrderAlertMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- หน้าเข้างานใช้ navigator.geolocation ใน WebView — Capacitor จะขอ permission
         ระดับแอปให้อัตโนมัติเมื่อประกาศไว้ที่นี่ (ไม่งั้น WebView ปฏิเสธ GPS เงียบ ๆ) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Push notification (Android 13+ ต้องขอ runtime ด้วย — plugin จัดการให้) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- ไมโครโฟน: ปุ่มสั่งเสียง (ตัวแปลงเสียงของเครื่อง) + ผู้ช่วยหลังร้าน/AI Live (getUserMedia ใน WebView)
         ไม่ประกาศ = WebView ปฏิเสธไมค์เงียบ ๆ ใช้เสียงในแอปไม่ได้เลย -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
</manifest>
