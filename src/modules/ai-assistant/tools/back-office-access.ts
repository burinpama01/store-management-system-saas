/**
 * ผู้ช่วยหลังร้านใช้ได้เฉพาะ "เจ้าของหรือผู้จัดการ"
 *
 * `catalog.manage` คือสิทธิ์ที่ตรงกับสองบทบาทนี้พอดีในตารางสิทธิ์ของระบบ (owner, manager)
 * — cashier และ staff ไม่มี จึงใช้เป็นด่านเดียวของฟีเจอร์นี้ได้ทั้งชุด
 *
 * **ต้องใส่กับ tool หลังร้านทุกตัว รวมตัวที่ดูเหมือนไม่เกี่ยวกับแคตตาล็อก** เช่นบัญชี:
 * `cashflow.record` มีครบทุกบทบาทรวม staff ถ้าใช้อย่างเดียวจะกลายเป็นว่าพนักงานหน้าร้าน
 * สั่งผู้ช่วยลงบัญชีได้ ซึ่งไม่ใช่สิ่งที่ตกลงกันไว้
 *
 * มีเทสล็อกว่าบทบาท cashier/staff ต้องไม่มีสิทธิ์นี้ — ถ้าวันหนึ่งตารางสิทธิ์เปลี่ยน
 * เทสจะแดงก่อนที่ฟีเจอร์จะเปิดให้คนที่ไม่ควรใช้
 */
export const BACK_OFFICE_ASSISTANT_PERMISSION = "catalog.manage" as const;
