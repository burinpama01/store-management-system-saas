import type { Role } from "@/modules/tenants/types";

/**
 * ผู้ช่วยหลังร้านใช้ได้เฉพาะ "เจ้าของหรือผู้จัดการ"
 *
 * `catalog.manage` คือสิทธิ์ที่ตรงกับสองบทบาทนี้พอดีในตารางสิทธิ์ของระบบ (owner, manager)
 * — cashier และ staff ไม่มี จึงใช้เป็นด่านเดียวของฟีเจอร์นี้ได้ทั้งชุด
 *
 * **ต้องใส่กับ tool หลังร้านทุกตัว รวมตัวที่ดูเหมือนไม่เกี่ยวกับแคตตาล็อก** เช่นบัญชี:
 * `cashflow.record` มีครบทุกบทบาทรวม staff ถ้าใช้อย่างเดียวจะกลายเป็นว่าพนักงานหน้าร้าน
 * สั่งผู้ช่วยลงบัญชีได้ ซึ่งไม่ใช่สิ่งที่ตกลงกันไว้
 *
 * มีเทสล็อกว่าบทบาท cashier/staff ต้องไม่มีสิทธิ์นี้ — ถ้าวันหนึ่งตารางสิทธิ์เปลี่ยน
 * เทสจะแดงก่อนที่ฟีเจอร์จะเปิดให้คนที่ไม่ควรใช้
 */
export const BACK_OFFICE_ASSISTANT_PERMISSION = "catalog.manage" as const;

/**
 * บทบาทที่สั่ง "ลงรายรับ-รายจ่าย" ผ่านผู้ช่วยได้
 *
 * แคชเชียร์อยู่ในลิสต์นี้ด้วยโดยตั้งใจ — งานลงบัญชีหน้าร้านเป็นงานประจำของเขา แต่ให้
 * ใช้ได้เฉพาะเรื่องบัญชี และปุ่มโผล่เฉพาะหน้าบัญชีเท่านั้น (ดู layout)
 *
 * **ทำไมต้องกั้นด้วยบทบาท ไม่ใช่สิทธิ์:** `cashflow.record` มีครบทุกบทบาทรวม staff
 * ถ้ากั้นด้วยสิทธิ์อย่างเดียว พนักงานทั่วไปจะสั่งลงบัญชีได้ด้วย ซึ่งไม่ใช่สิ่งที่ตกลงไว้
 * และสองบทบาทนี้ถือสิทธิ์เกือบชุดเดียวกัน จึงแยกด้วยสิทธิ์ไม่ได้เลย
 */
export const ACCOUNTING_ASSISTANT_ROLES = [
  "super_admin",
  "owner",
  "admin",
  "manager",
  "cashier",
] as const satisfies readonly Role[];

/** แคชเชียร์เห็นปุ่มเฉพาะหน้านี้ — ที่อื่นไม่มีอะไรให้เขาสั่งอยู่แล้ว */
export const CASHIER_ASSISTANT_PATH_PREFIX = "/accounting";
